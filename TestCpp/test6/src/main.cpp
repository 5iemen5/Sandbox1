#include "main.h"
#include <vector>
#include <algorithm>

int arr[] = {1, 2, 3,4 };

void func(std::vector<int> & temp, auto first, auto last){

    //for(int i = 0; i < temp.size; i++){

        for(auto j = 0; j < last; j++){
            temp.push_back()
        }
        
   // }

}

int main() {
    
    std::vector<int> arr = {1,2,3,4,5};
    std::reverse(arr.begin(), arr.end());
    


    return 0;
}
